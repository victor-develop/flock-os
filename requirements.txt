# flock_os additional Python dependencies.
#
# NOTE: do NOT list `frappe` here. The Frappe framework is provided by the
# bench environment (installed at bench init), and declaring it would force a
# conflicting re-resolution during `bench get-app`. List only the flock_os
# app's OWN third-party deps here as the domain grows.
